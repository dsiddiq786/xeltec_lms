# =============================================================================
# AI Course Generator - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# OpenAI Configuration
# -----------------------------------------------------------------------------
# Your OpenAI API key for content generation
OPENAI_API_KEY=sk-your-openai-api-key-here

# Model to use for text generation (gpt-4-turbo recommended for quality)
OPENAI_MODEL=gpt-4-turbo

# -----------------------------------------------------------------------------
# DALL-E Image Generation Configuration
# -----------------------------------------------------------------------------
# Model for image generation
DALLE_MODEL=dall-e-3

# Image size: 1024x1024, 1024x1792, 1792x1024
DALLE_SIZE=1024x1024

# Image quality: standard, hd
DALLE_QUALITY=standard

# -----------------------------------------------------------------------------
# TTS (Text-to-Speech) Configuration
# -----------------------------------------------------------------------------
# TTS model: tts-1 (standard), tts-1-hd (high quality)
TTS_MODEL=tts-1

# Voice: alloy, echo, fable, onyx, nova, shimmer
TTS_VOICE=alloy

# Audio format: mp3, opus, aac, flac
TTS_RESPONSE_FORMAT=mp3

# -----------------------------------------------------------------------------
# MongoDB Configuration
# -----------------------------------------------------------------------------
# MongoDB connection string
# Local: mongodb://localhost:27017
# Atlas: mongodb+srv://user:password@cluster.mongodb.net
MONGODB_URI=mongodb://localhost:27017

# Database name for storing courses
MONGODB_DATABASE=ai_course_generator

# Collection name for course documents
MONGODB_COLLECTION=courses

# -----------------------------------------------------------------------------
# Worker Configuration
# -----------------------------------------------------------------------------
# Number of concurrent course generation jobs
WORKER_CONCURRENT_JOBS=3

# Thread pool size for media generation (images + TTS)
MEDIA_THREAD_POOL_SIZE=6

# Maximum memory usage before forced cleanup (MB)
WORKER_MAX_MEMORY_MB=1024

# -----------------------------------------------------------------------------
# File Storage Configuration
# -----------------------------------------------------------------------------
# Base directory for generated course files (defaults to ./Generated_Courses)
# GENERATED_COURSES_DIR=/path/to/generated/courses

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Environment: development, staging, production
ENVIRONMENT=development

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
